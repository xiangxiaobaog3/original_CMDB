
from src.engine.base import BaseHandler

class AgentHandler(BaseHandler):

    def handler(self):
        """
        import subprocess

        res = subprocess.getoutput(cmd)
        return res

        :return:
        """

        print('agent')